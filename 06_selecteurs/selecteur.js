//page sur les sélecteurs

// avec cet évènement window.onload toute la pageé est chargée et cela permet de mettre le script dans head sans async ou defer
window.onload = function(){
    var select1 = titreN1;
    console.info(select1);
}
